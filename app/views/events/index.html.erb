<div class="text-center">
  <h1> Tous les Events</h1>
  <ul>
    <% @events.each do |event| %>
    <li>
      <%= event.name %>
      <%= event.description %>
      <%= event.reviews %>
    <% end %>
    </li>
  </ul>
</div>

